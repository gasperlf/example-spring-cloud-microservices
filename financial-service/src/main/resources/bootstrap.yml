spring:
  profiles:
    active: application
  cloud:
    config:
      uri: ${config-server.uri}
      name: financial
      enabled: true
      username: user-server-config
      password: 123456789!#