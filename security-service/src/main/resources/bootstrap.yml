spring:
  profiles:
    active: application, database
  cloud:
    config:
      uri: ${config-server.uri}
      name: security
      enabled: true
      username: user-server-config
      password: 123456789!#