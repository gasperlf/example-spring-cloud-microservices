spring:
  profiles:
    active: application
  cloud:
    config:
      uri: ${config-server.uri}
      name: functions
      enabled: true
      username: user-server-config
      password: 123456789!#