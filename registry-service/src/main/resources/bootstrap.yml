spring:
  profiles:
    active: application
  cloud:
    config:
      uri: ${config-server.uri}
      name: registry
      enabled: true
      username: user-server-config
      password: 123456789!#